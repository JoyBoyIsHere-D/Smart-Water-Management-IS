# Root .env — used by docker-compose for shared variables
# Copy this to .env and fill in your values

# ─── Database ────────────────────────────────────────────────────────────────
DB_USER=wateradmin
DB_PASSWORD=changeme_strong_password
DB_NAME=water_management
